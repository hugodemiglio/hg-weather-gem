require 'spec_helper'

describe HG::Weather do
  it 'has a version number' do
    expect(HG::Weather::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
